spring.application.name=logmeet

spring.profiles.include=aws
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.default_batch_fetch_size=100
spring.jpa.database=mysql

#alter table
spring.jpa.generate-ddl=true

logging.level.org.hibernate.SQL=debug
logging.level.org.hibernate.orm.jdbc.bind=debug
logging.level.org.springframework.security=debug
logging.level.org.focus.logmeet=debug

spring.jackson.time-zone=Asia/Seoul
spring.jpa.properties.hibernate.jdbc.time_zone=Asia/Seoul


# JWT
secret.jwt-secret-key=${JWT_SECRET_KEY}

# 404 에러 처리
spring.web.resources.add-mappings=false

# Swagger 기본 응답 메시지 비활성화
springdoc.override-with-generic-response=false

# Flask 서버 주소
flask.server.url=http://172.31.43.235:5001

# Elastic 서버 주소
spring.elasticsearch.uris=https://1a314713817c41978cf9096b50462844.ap-northeast-2.aws.elastic-cloud.com:443
spring.elasticsearch.username=elastic
spring.elasticsearch.password=gvxSiB1y4idz2LYK7bxQySw3


# CI/CD 환경 용도
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

cloud.naver.credentials.accessKey=${NCLOUD_ACCESS_KEY}
cloud.naver.credentials.secretKey=${NCLOUD_SECRET_KEY}
